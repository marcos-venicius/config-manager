Transform this tool in an engine so that you can create you
config script and just use the engine to execute it.

Create some kind of DSL (why not?) so we can create agnostic pipelines
and allow it to run over this engine.
